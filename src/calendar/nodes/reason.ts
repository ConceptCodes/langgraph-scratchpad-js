import type { AgentStateAnnotation } from "../agent/state";

// NOTE: after gatering requirements lets reason on what the user is requsting from
export const reasonNode = async (
  state: typeof AgentStateAnnotation.State
) => {};
